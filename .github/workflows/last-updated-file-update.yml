name: Update LAST_UPDATED on Terms and Conditions Change

on:
  push:
    branches: [ sg/last-updated-workflow ]
    paths:
      - UserGuide/POC-Terms-And-Conditions.md
      - fr/UserGuide/Conditions-générales-de-POC.md

permissions:
  contents: write

jobs:
  update-last-updated:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Update LAST_UPDATED file
        run: |
          echo "LAST UPDATED: $(date -u +'%Y-%m-%dT%H:%M:%SZ')" > LAST_UPDATED

      - name: Commit and push LAST_UPDATED
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add LAST_UPDATED
          git commit -m "Update LAST_UPDATED to release date"
          git push
